import { describe, expect, test } from "@jest/globals";

describe("part1", () => {
  test("calculates example line 1", () => {
    expect(1 + 1).toBe(2);
  });
});

describe("part2", () => {
  test("calculates example line 1", () => {
    expect(1 + 1).toBe(2);
  });
});
